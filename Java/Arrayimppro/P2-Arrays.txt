import java.util.Arrays;

public static void arr(){
        int[] arr=new int[3]; //fixed size array declared;
        arr[0]=33;
        arr[1]=1;
        arr[2]=5;
        System.out.println(arr);//ref value
        System.out.println(Arrays.toString(arr));//prop value
        Arrays.sort(arr);//to sort array
        System.out.println(Arrays.toString(arr));//prop value
    }